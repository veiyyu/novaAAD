# novaAAD v5 (self-contained: bundles the NOVA2026 auditory engine).
# Offline path (convert / train / replay / evaluate / build the demo):
numpy
scipy
soxr
mne
# Live path (scripts.auditory.live on a real cap via StreamSession/LSL) also needs:
mne-lsl
# Live audio output (scripts.auditory.live device playback) also needs:
sounddevice
# Calibration session (tools/calibration_session.py, tools/xdf_to_trial.py):
# capture via LabRecorder (https://github.com/labstreaminglayer/App-LabRecorder,
# installed separately -- not a pip package) and convert its .xdf output.
pyxdf
